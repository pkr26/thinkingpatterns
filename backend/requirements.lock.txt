# Pinned runtime dependencies — exact versions the 258-test suite ran
# against. The Docker image installs with --no-deps so this file is the
# complete supply chain: nothing resolves at build time.
# Regenerate after dependency changes with the versions from the tested
# environment (importlib.metadata) and re-run the full test suite.
aiosqlite==0.22.1
annotated-types==0.8.0
anyio==4.15.0
asyncpg==0.30.0
certifi==2026.7.22
click==8.5.0
cryptography==50.0.1
fastapi==0.141.1
greenlet==3.5.5
h11==0.16.0
httpcore==1.0.9
httptools==0.8.0
httpx==0.28.1
idna==3.19
pydantic==2.13.5
pydantic-core==2.46.5
python-dotenv==1.2.3
sqlalchemy==2.0.52
starlette==1.6.0
typing-extensions==4.16.0
uvicorn==0.52.4
uvloop==0.22.1
watchfiles==1.2.0
websockets==17.1
